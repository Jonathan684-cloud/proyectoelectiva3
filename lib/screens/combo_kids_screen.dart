import 'package:flutter/material.dart';

class ComboKidsScreen extends StatelessWidget {
  const ComboKidsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Combo Niños'),
      ),
      body: const Center(
        child: Text('Combo Niños'),
      ),
    );
  }
}
